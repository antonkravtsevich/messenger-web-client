import React from 'react';
import MainApp from './components/main_app/MainApp'

var App = new React.createClass({
  render: function() {
    return (
      <MainApp />
    );
  }
});

export default App;
